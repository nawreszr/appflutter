# eureka-server

Ce dossier accueillera le serveur de découverte Eureka.

## Objectif

- Projet Spring Boot avec `@EnableEurekaServer`
- Enregistrement des services `etudiant-service` et `grading-service`
- Point de découverte accessible sur le port `8761`
